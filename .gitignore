# Cài đặt cá nhân và cache của các IDE (Visual Studio, VS Code)
.vs/
.vscode/
*.vcxproj.user
*.suo

# Thư mục build và các file do trình biên dịch tạo ra
# Các quy tắc này được thêm dấu / ở đầu để chỉ áp dụng ở thư mục gốc
# Điều này sẽ không ảnh hưởng đến thư mục x64/ bên trong vendor/
/[Bb]uild/
/[Dd]ebug/
/[Rr]elease/
/x64/
/x86/
**/[Oo]bj/
**/[Bb]in/

# Các loại file thực thi, thư viện, và file trung gian
*.exe
*.dll
*.obj
*.ilk
*.pdb
*.tlog
*.lib

# NGOẠI LỆ: KHÔNG bỏ qua các file .lib bên trong thư mục vendor
# Quy tắc này bây giờ sẽ hoạt động chính xác vì thư mục /vendor/x64/ không còn bị bỏ qua
!vendor/**/*.lib

# Các file hệ thống của macOS và Windows
.DS_Store
Thumbs.db